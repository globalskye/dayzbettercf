# Скопируй в .env и заполни значениями из браузера (DevTools → Application → Cookies)
# В Docker используется только режим токена (без headless-браузера)
# Для входа в админку добавь ADMIN_USER и ADMIN_PASS (иначе бэкенд может не создать админа и логин даст 502/ошибку)

# CFtools (обязательно для Docker)
CFTOOLS_CDN_AUTH=eyJ...
CFTOOLS_SESSION=.eJ...
CFTOOLS_USER_INFO="{\"profile\": {...}, \"status\": true}"
CFTOOLS_CF_CLEARANCE=RkLKZ...
CFTOOLS_ACSRF=EGPsxjU...

# Админка (логин/пароль при первом запуске создают пользователя admin)
ADMIN_USER=admin
ADMIN_PASS=admin
JWT_SECRET=change-me-in-production
