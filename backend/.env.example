# Server
PORT=8080
ENV=development

# App auth (роли: admin, editor, viewer)
# При первом запуске создаётся admin, если указаны логин и пароль
JWT_SECRET=change-me-in-production
ADMIN_USER=admin
ADMIN_PASS=admin

# CFtools API — два режима:

# Режим 1: Токен из браузера (DevTools → Application → Cookies)
CFTOOLS_CDN_AUTH=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
CFTOOLS_SESSION=.eJxtzjsOAjEMRdG9...
CFTOOLS_USER_INFO="{\"profile\": {\"display_name\": \"...\"}, \"status\": true}"
CFTOOLS_CF_CLEARANCE=RkLKZ.OV1_CnG8sCrn7u25s9vX9E0vqkPGwSEi5rOZs-...
# CFTOOLS_ACSRF=... — опционально, если не подхватывается автоматически

# Режим 2: Автологин (identifier + password hash)
# CFTOOLS_IDENTIFIER=bvr9
# CFTOOLS_PASSWORD_HASH=sha256_hash_of_your_password

# DATABASE_URL=file:dayzsmartcf.db — SQLite по умолчанию
# CFTOOLS_HEADLESS=false — показать браузер при Cloudflare (режим 2)

# SEED_SAMPLE=1 — при старте добавить примерного игрока (ExamplePlayer) с историей онлайна для демо
